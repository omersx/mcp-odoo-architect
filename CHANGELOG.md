# Changelog

## 0.2.0

- New `save_addon` tool: generate + write the 11-file starter to `output_dir/<module>/`
- Path allowlist + traversal guard on disk writes
- Generic briefs compose custom fields/tests instead of nearest-preset reuse
- Publish polish: `files` manifest, tool docs

## 0.1.0

- Initial stdio MCP server: `draft_plan`, `generate_addon`, `validate_addon`, `explain_tradeoffs`, `list_presets`
- Deterministic local composer, no keys or network
